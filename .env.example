# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Public App URL (used for magic links and email templates)
PUBLIC_APP_URL=https://your-app-domain.com

# JWT Secret for Magic Links (generate a secure random string)
JWT_SECRET=your_super_secure_jwt_secret_key_here

# Note: RESEND_API_KEY, FROM_EMAIL, and FROM_NAME are already configured
# in the existing system for waitlist confirmation emails

# GitHub Secrets (for CI/CD)
# Add these to your GitHub repository secrets:
# - VITE_FIREBASE_API_KEY
# - VITE_FIREBASE_AUTH_DOMAIN
# - VITE_FIREBASE_PROJECT_ID
# - VITE_FIREBASE_STORAGE_BUCKET
# - VITE_FIREBASE_MESSAGING_SENDER_ID
# - VITE_FIREBASE_APP_ID
# - PUBLIC_APP_URL
# - JWT_SECRET

# Firebase Functions Secrets (for production)
# Set these using: firebase functions:secrets:set SECRET_NAME
# - PUBLIC_APP_URL
# - JWT_SECRET
# Note: RESEND_API_KEY, FROM_EMAIL, FROM_NAME are already configured
